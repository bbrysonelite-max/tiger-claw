# Tiger Claw — Things That Need Doing

**Generated:** 2026-03-12  
**Version:** v2026.03.07.11 — All 10 GAPs complete  
**Phase:** Pre-launch / Production readiness

---

## 🔴 Critical — Blocks Deploy

### 1. Fix API Build Error
The TypeScript build fails with `TS1107: Jump target cannot cross function boundary` in [health.ts:29](file:///Users/brentbryson/Tigerclaw-Anti_Gravity/tiger-claw/api/src/routes/health.ts#L29). A `break` statement is used inside a `try/catch` block (not inside a loop), which is invalid. The API **cannot be built or deployed** until this is fixed.

### 2. Rotate Compromised Serper Keys
3 Serper API keys were exposed in git history (now purged). They must be rotated at [serper.dev](https://serper.dev/api-key) and stored in Secret Manager as `SERPER_KEY_1`, `SERPER_KEY_2`, `SERPER_KEY_3`. Until rotated, `tiger_scout` uses compromised credentials.

### 3. Store Remaining Secrets in GCP Secret Manager
12 of 19 required secrets are **not yet stored**. The API will crash on startup without them:

| Secret | Status |
|--------|--------|
| `DATABASE_URL` | ⬜ After `terraform apply` |
| `REDIS_URL` | ⬜ After `terraform apply` |
| `PLATFORM_ONBOARDING_KEY` | ⬜ Needed |
| `PLATFORM_EMERGENCY_KEY` | ⬜ Needed |
| `STRIPE_SECRET_KEY` | ⬜ Needed |
| `STRIPE_WEBHOOK_SECRET` | ⬜ Needed |
| `STRIPE_PRICE_BYOK` | ⬜ Needed |
| `ADMIN_TOKEN` | ⬜ Needed |
| `ADMIN_TELEGRAM_BOT_TOKEN` | ⬜ Needed |
| `ADMIN_TELEGRAM_CHAT_ID` | ⬜ Needed |
| `TIGER_CLAW_HIVE_TOKEN` | ⬜ Needed |
| `SERPER_KEY_1/2/3` | ⚠️ Rotate first |

### 4. Run Encryption Key Re-Migration
`ENCRYPTION_KEY` was rotated on 2026-03-12. Before deploying to production, the re-encryption migration must be run against the production database:
```bash
OLD_ENCRYPTION_KEY=<old> NEW_ENCRYPTION_KEY=<from Secret Manager> DATABASE_URL=<prod> npx tsx ops/rotate-encryption-key.ts
```

### 5. Run `terraform apply`
GKE cluster, Cloud SQL, and Memorystore are defined in `ops/gcp-terraform/main.tf` but need to be applied to create the actual infrastructure. `DATABASE_URL` and `REDIS_URL` come from the Terraform output.

---

## 🟠 High — Required for Launch

### 6. Update Stripe/Stan Store Webhook URLs
External services still point to old `thegoods.ai` URLs:
- **Stripe Dashboard** → Developers → Webhooks → update to `https://api.tigerclaw.io/webhooks/stripe`
- **Stan Store** → update webhook URL to same endpoint
- Copy new `STRIPE_WEBHOOK_SECRET` (whsec_...) from Stripe → Secret Manager

### 7. Map Custom Domain to Cloud Run
`api.tigerclaw.io` must be mapped to the Cloud Run service after deploy. Without this, webhook URLs won't resolve.

### 8. Remove Mock Session Fallbacks
[subscriptions.ts](file:///Users/brentbryson/Tigerclaw-Anti_Gravity/tiger-claw/api/src/routes/subscriptions.ts) has hardcoded `http://localhost:3000/success?session_id=mock_session` fallbacks (lines 76, 88). These will break in production — should use `FRONTEND_URL` env var.

### 9. Anthropic References Still in Codebase
3 files still reference Anthropic (key prefix detection for validation). These were written for the old architecture:
- `api/src/tools/tiger_keys.ts` — key validation logic may test against `sk-ant-` prefix
- `api/src/tools/tiger_onboard.ts` — same
- `api/src/routes/keys.ts` — same

Need to confirm these correctly handle Google API keys (`AIza...`) since the platform now runs on Gemini.

### 10. Build Bot Pool to 1,000+ Tokens
Currently: **only 1 session**, **7 purchased numbers**, and the pipeline is struggling with SMS OTP delivery. Target is 1,000+ tokens before scaling. The voice call OTP fallback was just added — needs testing at scale.

### 11. Key Abuse Protection System — Not Implemented
CLAUDE.md documents a **locked, non-negotiable** 3-strike abuse policy for Layer 4 emergency key usage:
1. First incident → warning email
2. Second → stronger warning
3. Third → bot auto-paused, must add own key

No code tracks Layer 4 consumption counts or sends these emails. No email service is integrated at all.

### 12. Email Service — Not Integrated
Multiple flows require email (provisioning confirmation, key abuse warnings, onboarding instructions) but there is no email provider (SendGrid, SES, etc.) in the codebase. Only the data model has email fields.

---

## 🟡 Medium — Important for Quality

### 13. Wire Cloud Run Secret Manager into `cloudrun.yaml`
The YAML file lists secrets as comments but doesn't actually reference them via `secretKeyRef`. Secrets need to be wired as env vars from Secret Manager in the Cloud Run service definition.

### 14. Update `thegoods.ai` Marketing Site
[WEBSITE-UPDATE-SPEC-v1.md](file:///Users/brentbryson/Tigerclaw-Anti_Gravity/tiger-claw/specs/tiger-claw/WEBSITE-UPDATE-SPEC-v1.md) exists but hasn't been executed. `thegoods.ai` needs Tiger Claw branding/copy.

### 15. `CLAUDE.md` / `ai.ts` Description Mismatch
CLAUDE.md line 137 still says `ai.ts` "runs Anthropic tool loop" — but `ai.ts` is fully on Gemini (`@google/generative-ai`). The descriptions table should be updated.

### 16. E2E Test Suite — Single Test Only
`web-onboarding/e2e/onboarding.spec.ts` exists but there's only one spec file. No API-level integration tests beyond a single provisioner unit test. Critical flows (Stripe webhook → provision → Telegram message → AI response) have no automated testing.

### 17. Admin Dashboard — Spec Only
`specs/tiger-claw/DASHBOARD-PRD.md` (20KB spec) exists. GAP-3 marked the admin dashboard as "wired to live data" but it lives in a separate repo (`tiger-claw-ops` / `botcraftwrks.ai`). Needs verification that it actually works with the current API.

### 18. Customer Dashboard Route
`api/src/routes/dashboard.ts` exists (3KB) — verify it serves the customer dashboard correctly and is wired to the web-onboarding Next.js app's `/dashboard` page.

---

## 🔵 Low — Polish / Improvement

### 19. Consolidate Localhost Fallbacks
5 files in `web-onboarding/src/` use `process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:4000"`. This is correct for dev but should be set via `.env.production` to prevent any possibility of production traffic hitting localhost.

### 20. `deployment_state.json` Stale Commit Hashes
Build entries reference commit hashes from before the git history rewrite. These no longer resolve. Consider resetting with current hashes or accepting them as historical.

### 21. Legacy Phase Task Files
`tasks/PHASE-0.md` through `tasks/PHASE-4.md` are documented as **superseded** by the GAP system. Consider archiving or deleting to avoid confusion.

### 22. `tasks/todo.md` — Stale Content
Contains implementation plans for `tiger_onboard` and `tiger_score` that were completed in Feb 2026. Should be cleaned up or archived.

### 23. Disk Check Path in Health Endpoint
[health.ts L56](file:///Users/brentbryson/Tigerclaw-Anti_Gravity/tiger-claw/api/src/routes/health.ts#L56) checks `/home/ubuntu/customers` which doesn't exist on Cloud Run. Not harmful (falls back to `/`) but is dead code.

### 24. `super-bowl-ad/` Directory
96-file directory at repo root — unclear if this is project-related or should be in a separate repo.

---

## 📋 Summary

| Priority | Count | Status |
|----------|-------|--------|
| 🔴 Critical (blocks deploy) | 5 | Must fix before any production deploy |
| 🟠 High (required for launch) | 7 | Must complete before accepting real customers |
| 🟡 Medium (quality) | 6 | Should do before public launch |
| 🔵 Low (polish) | 6 | Nice to have, do when time permits |
| **Total** | **24** | |

> [!IMPORTANT]
> The single most impactful action right now is **fixing the build error in health.ts** (#1) — until that's fixed, nothing else can ship.
